import {AuthPermission} from '@tangential/authorization-service'

export const cleanupSnapCaptures = function (): Promise<AuthPermission[]> {
  return Promise.all([])
}
export const cleanupReports = function (): Promise<AuthPermission[]> {
  return Promise.all([])

}
